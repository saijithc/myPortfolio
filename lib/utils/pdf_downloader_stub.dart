import 'dart:typed_data';

Future<bool> downloadPdf(Uint8List bytes, String fileName) async {
  // Stub implementation for platforms not yet supported
  throw UnsupportedError('PDF download not supported on this platform');
}
